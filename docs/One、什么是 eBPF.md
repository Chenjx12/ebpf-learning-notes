# One、什么是 eBPF

date: 2026.5.22

用一句话简单的概括定义：`eBPF` 是一项让你能在不修改内核源码、不加载内核模块（LKM）的情况下，`安全、高效`地在 **Linux 内核** 中运行 `自定义` 代码的技术。

可以简单的理解为 Linux内核的“JavaScript”。在 eBPF 之前，内核是个黑盒，想要改变它的行为只能编写复杂的内核模块（像写 C/S 架构的服务端）；而现在，内核暴露了钩子，让我们可以写脚本动态注入进去（像写前端 JS 一样），内核帮你安全的执行。

结合二进制传统的 Pwn 来分析，eBPF 最 **核心** 的 4 个基础概念如下：

## 核心机制：事件驱动

传统的 Pwn 是找漏洞、劫持控制流；而 eBPF 是 **被动的、事件驱动的** 。

我们不能随便写一个死循环丢在内核里面让它自己去跑，我们只能 `订阅` 内核里的特定事件，当事件发生时，内核会 `回调` 我们写好的 eBPF 程序.

下面是几种最重要的 **订阅方式（挂载点）**：

- kprobe/kretprobe：挂钩内核函数的入口和返回。比如挂钩 `vfs_read` ，只要内核读文件就触发。灵活，但是属于 **不稳定API** ，内核升级可能会导致函数变化。
- tracepoint：内核开发者静态预留的追踪点（比如 `sys_enter_execve` ）。**非常稳定** ，内核升级也不用担心，但是只能挂在内核提前声明的地方。
- uprobe：挂钩用户态程序的函数。比如挂钩 `libc` 的 open 函数。
- XDP/TC：网络层面的挂载点，这是搞云原生网络（如Cilium）的核心，能在网卡驱动层处理数据包，速度极快。



## 安全保障：Verifier（验证器）—— PWN 手的噩梦，工程的福音

这时候肯定会有一个疑问了：我动了内核，往里面随便注入代码，万一引发 `Panic` 怎么办？

这正是 eBPF 最厉害的地方——它有一个 `Verifier` （验证器）。

在我们把 eBPF 字节码加载进内核时，Verifier 会在加载前进行 **极其严苛** 的静态代码分析，它必须确保：

1. 程序一定会终止：不允许不可控的循环（早期甚至不允许循环，现在允许有界循环），防止卡死内核。
2. 内存访问绝对安全：所有的指针访问必须经过 **边界检查** （类似于强行开启了各种 Stack Canary 和边界保护），绝不能越界读写内核程序。
3. 权限最小化：只允许调用内核白名单里的 `Helper函数` （比如 `bpf_probe_read` 读取内存， `bpf_get_current_pid_tgid` 获取进程号），绝不能随便调用内核内部函数。

如果 PWN 手的思维是“如何绕过保护”，那学 eBPF 时的思维就是“如何满足 Verifier 的要求让它放行“，

这些严苛的检查只为指向一个结果：一旦 eBPF 程序加载成功，它绝对不会导致内核崩溃。



## 性能飞轮：JIT编译

eBPF 程序是我们用 C语言编写的，编译成 BPF 字节码后传递给内核。

如果内核每次触发事件都去解释执行字节码，那性能会很差。

所以内核使用了 JIT（Just-In-Time）编译技术。在 Verifier 验证通过后，内核会把我们所编写的 BPF 字节码 **直接翻译成当前 CPU 架构的原生机器码** （比如 x86_64 汇编）。这意味着我们写的 eBPF 代码，最终是以近乎原生内核代码的速度在跑，这也是为什么 eBPF 能做到 CPU 开销 ＜ 5% 的原因。



## 数据流通：Maps 与 Helper函数

eBPF 程序运行在内核态，它不能做任何可能阻塞或严重影响性能的 I/O，因此不能“随便 print”；能用的打印要么是极度受限的调试接口（bpf_printk），要么是走 map 的异步事件通道。

那数据怎么传出来给你看？怎么保存状态？这就靠 Maps 和 Helper 函数。

- **Helper 函数**：eBPF 程序调用的“系统调用”。比如你想获取当前进程的 PID，用 `bpf_get_current_pid_tgid()`；想安全读取内核内存，用 `bpf_probe_read()`。
- **Maps（映射表）**：这是内核态和用户态共享数据的核心。你可以把 Maps 想象成内核里的一块共享内存（键值对结构）。eBPF 程序在内核里往 Maps 中写数据（比如统计某个 IP 的访问次数），你的用户态 Python 程序在后台不停地读 Maps，这就实现了数据交换。你计划里提到的 `Ring Buffer` 就是 Maps 的一种，专门用来高性能地单向传递事件流。